import { makeStyles } from "@mui/styles";

export const postCard = makeStyles({
  cardContainer: {
    width: "470px",
    boxShadow: "none",
  },
  cardActionRight: {
    marginLeft: "65%!important",
  },
});
